name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the app
      run: xcodebuild -scheme CheckCICD -configuration Release
